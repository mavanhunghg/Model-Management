spring:
  datasource:
    url: jdbc:mysql://localhost:3306/model_mgmt_db
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true

server:
  port: ${SERVER_PORT:8081}

services:
  statistics:
    base-url: ${STATISTICS_BASE_URL:http://localhost:8082}
